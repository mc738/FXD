namespace FXD

module DocumentGenerator =
    
    let run () =
        
        // 1. create general indexes.
        
        // 2. create pages
        
        // 2.a. substitute specific indexes for page
        
        // 3. create code docs (based on namespace/modules).
        
        // 3.a. for each module/namespace create a page (including sub ones).
        
        // 3.b. add classes, functions, records, unions to relevant page.
        
        // 3.c. substitute specific indexes for page
        
        // Save all in /docs/ directory (all files can reference each other in here).
        
        // Special - skip modules (modules to be skipped).
        
        ()
    

